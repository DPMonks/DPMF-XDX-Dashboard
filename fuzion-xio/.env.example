# Optional. Needed only to mint new files to Infura IPFS from this app.
# Xaman API key/secret belong on fuzion-xio-api (.env), never here (no VITE_ prefix).
VITE_INFURA_IPFS_PROJECT_ID=
VITE_INFURA_IPFS_PROJECT_SECRET=

# Override if you stand up a local API later.
# VITE_API_ORIGIN=https://fuzion-xio.com
